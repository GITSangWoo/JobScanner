FROM apache/airflow:2.7.1-python3.10

USER root
RUN apt-get update && apt-get install -y curl wget

# airflow 사용자로 변경 후 pip 패키지 설치
USER airflow
RUN pip install boto3

# dags 디렉토리 복사
COPY dags /opt/airflow/dags
